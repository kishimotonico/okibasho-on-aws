## Git

- コミットメッセージは日本語の Conventional Commits。本文に経緯と目的を書く
- 実装は main からブランチを切る。docs だけの整備は main 直コミット可
- プッシュはしない

## ドキュメントとコメント

- docs は最小限にし、読みやすいコードで説明する。正本は `docs/architecture.md`（設計）、`docs/deploy.md`（手順）、`docs/roadmap.md`（残タスク）、管理UI は `packages/web/PRODUCT.md` と `DESIGN.md`
- 調べれば分かる一般的な挙動や過去の経緯は書かない。検討メモは実装後に消すか正本へ要点だけ移す
- コメントはコードから分からない WHY / WHY NOT だけ。経緯や docs へのリンクは書かない

## コードの直し方

- 差分の小ささではなく直した後のコードで判断する。目安は、状態と条件分岐が少ない、責務の置き場所が一つ、処理を上から読んで追える。作業量やリスクは考慮しない
- 本番未公開なので、起きる見込みの薄いケースへの防御やフォールバックは足さない

## 管理UI（packages/web）

- 防衛的なテストは書かない。見た目と操作は実機で確認する

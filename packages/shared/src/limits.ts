const MB = 1024 * 1024;

/** 1ファイルあたりの上限（50 MB） */
export const MAX_FILE_SIZE = 50 * MB;

/** 1ページ合計の上限（200 MB） */
export const MAX_PAGE_SIZE = 200 * MB;

/** 1ページあたりのファイル数上限 */
export const MAX_FILE_COUNT = 200;
